#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../grunt-jsdoc/bin/grunt-jsdoc" "$@"
else 
  exec node  "$basedir/../grunt-jsdoc/bin/grunt-jsdoc" "$@"
fi
